<h1>Burgers!</h1>

<h2>Not Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">What kind of burger would you like?:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devour">Sleepy?</label><br>
    <input type="radio" name="devour" value="1" checked> Not Devoured!<br>
    <input type="radio" name="devour" value="0"> Devoured
  </div>


  <button type="submit">Add Burger</button>
</form>